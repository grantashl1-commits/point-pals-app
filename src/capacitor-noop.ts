/**
 * No-op module for packages that are not needed in the Capacitor SPA build.
 * 
 * TanStack Start SSR packages are imported only for type augmentation
 * (routeTree.gen.ts declare module) and are not actually used in the SPA.
 * This file replaces those imports with a harmless stub.
 */
export {};
